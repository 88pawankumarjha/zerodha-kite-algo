#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -ne 3 ]; then
  echo "Usage: ./sell20 SYMBOL QTY SELL_PRICE" >&2
  echo "Example: ./sell20 SENSEX2651475400CE 20 15" >&2
  exit 2
fi

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

if [ -x ".venv/bin/python" ]; then
  PYTHON=".venv/bin/python"
else
  PYTHON="python3"
fi

exec "$PYTHON" -m src.sell_option_with_sl \
  --exchange BFO \
  --symbol "$1" \
  --qty "$2" \
  --sell "$3" \
  --product NRML \
  --stop-loss-percent 20
